const valEcran = document.querySelectorAll(".ecranInitial");
const numar = document.querySelectorAll(".sapte");

numar.addEventListener("keyup", function () {
  paragraph.innerNumber = numar.value;
});
